# ── Python ──
.venv/
venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# ── Secrets & env ──
.env
.env.*

# ── RAG / build artifacts (rebuilt on demand, not shipped) ──
prompts.json            # 6MB legacy corpus — kept locally, never pushed
lancedb_store/
.lancedb/
*.parquet
embedding_cache.pkl
.corpus_cache.json
bench/results/

# ── VS Code extension binaries ──
vscode-extension/out/
vscode-extension/node_modules/
vscode-extension/*.vsix
vscode-extension/*.png

# ── Editors / OS / scratch ──
.DS_Store
.idea/
*.log
*.bak
*.tgz
*.thumbnail
Claude outputs/

# stray binaries (HF rejects non-LFS binaries)
*.png
*.jpg
*.jpeg
*.gif
*.pkl
*.zip
*.ico
